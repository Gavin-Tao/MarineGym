# AEI 实验总结（2026-08-25，全部跑完）

## 0. 一句话

**六组对比里，碰撞率无一例外全部打平。** 这套方法在当前场景下不改变安全性，
唯一稳健的收益是「执行更平滑」（soft blend）与「绕路更短」（预测门控）。
根本障碍是场景本身没有安全压力，不是方法不行。

---

## 1. 消融矩阵（全部对齐，每格只差一个开关）

|  | 斜坡接管 (soft) | 阶跃接管 (binary) |
|---|---|---|
| **预测门控** | **RP-PSF (ours)** | Predictive + binary |
| **几何门控** | Geometric + prop. | Geometric + binary |

四格共同点：边界 0.6、观测 45 维、K=128/N=20/H=15、seed=0、120 iters、
其余超参见 `scripts/aei_fixed_params.sh`。另有 A-only（关滤波）与 PPO baseline。

评测：ood 场景，每格 10 个独立评测 seed。

---

## 2. 六组对比的完整结果

**碰撞率在全部六组里都不显著。** 下表只列显著项。

### 接管律（斜坡 vs 阶跃）—— 两行都做了，结论一致

| 对比 | 阶跃赢 | 斜坡赢 |
|---|---|---|
| 预测门控行 | detour (d=13.0)、return (d=1.8) | smoothness (d=3.9)、tracking (d=3.0)、correction (d=1.0) |
| 几何门控行 | detour (d=8.9) | smoothness (d=3.0)、tracking (d=1.4) |

**模式在两行都复现** —— 斜坡换来更平滑的执行与更准的跟踪，阶跃换来更短的路径。
这是今天最稳健的发现。

### 门控信号（预测 vs 几何）—— 两列都做了

| 对比 | 预测赢 | 几何赢 |
|---|---|---|
| 斜坡接管列 | detour (d=5.8) | — |
| 阶跃接管列 | detour (d=10.2)、return (d=1.6) | tracking (d=1.3) |

**预测门控在两列都显著减少绕路** —— 组件 A 的价值在此，也复现了。

### 关键对照：ours vs A-only（只报警、不滤波）

| 指标 | ours | A-only | 判定 |
|---|---|---|---|
| collision | 0.0169 | 0.0166 | **打平** |
| return | 29.51 | 30.48 | A-only 赢 (d=1.9) |
| detour | 0.2215 | 0.1985 | A-only 赢 (d=2.6) |
| tracking | 1.3484 | 1.3508 | ours 赢 (d=1.1) |

**这一格最要命：加上 MPPI 滤波（组件 B）没有带来任何安全收益，反而损失任务性能。**
在当前场景下，B 没有挣到它的位置。

### ours vs PPO baseline

collision / return / detour / tracking **全部打平**；ours 仅在 smoothness 上赢 (d=2.4)。

---

## 3. 为什么全都打平：场景没有安全压力

| 场景 | 一条 episode 内最近距离均值 | PPO 碰撞率 |
|---|---|---|
| nominal | 2.29 m | 0.8% |
| **ood** | **6.68 m** | 1.6% |
| hard | −0.04 m | **75%** |

ood 名义上更难（障碍速度 2.2~2.8 vs 0.4~0.9），但速度太快反而**擦身而过**，
最近距离比 nominal 还远一倍多。滤波器只在 1.8% 的步上介入。

hard 则相反，标定过头进了**饱和区**：两臂碰撞率都 0.75、最近距离为负，全都失败。

**结论：现有三个场景没有一个能测出安全性差异。** 这不是方法的问题，是评测设计的问题。

检验功效（`data/power.csv`）：nominal 每组 1405 episode 只能检出「碰撞率下降 89% 以上」，
ood 是 67%。而重新标定到 PPO 碰撞率 0.2~0.4 的场景，每组只需 121 个 episode
就能检出 50% 的下降 —— 现有数据量富余 10 倍。

---

## 4. 已确认但未修的问题：PPO 训练目标错配

`ppo.py:220` 的重要性比率是

```
ratio = exp( log π_θ(u_t) − log π_old(u_rl) )
              ↑ 滤波后动作     ↑ 采样动作
```

分子分母是两个不同的动作（`track.py:519` 把 u_rl 覆盖成了 u_t，
而 `sample_log_prob` 仍是采样时记的 log π(u_rl)）。判据：epoch 0 时所有步 ratio 应恰为 1，
现在滤波生效的步上 ≠1。

正确做法（表述 A）：shield 属于环境，学习器存 `u_rl`，`u_t` 只给仿真器执行。
因为 shield 不含 θ，它在 ∇log p_θ(τ) 里直接消失，score 只能是 ∇log π_θ(u_rl|s)。

**影响不均匀，且沿消融轴单调**：

| 格子 | u_t | 训练期介入 | 扭曲 |
|---|---|---|---|
| PPO / A-only | u_rl | — | 无 |
| 斜坡两格 | u_rl + λ(u_safe−u_rl)，λ 小 | 0.046 / 0.030 | 轻微 |
| 阶跃两格 | u_safe（全量替换） | 0.116 | **严重** |

所以「斜坡 vs 阶跃」的差异里，有多少来自部署时的接管律、有多少来自训练目标被扭曲，
**目前分不开**。而「预测 vs 几何」在斜坡列内扭曲程度相近（0.046 vs 0.030），相对可信。

---

## 5. 失效／作废的产出

| 项 | 状态 |
|---|---|
| N1 名义模型单步误差 | config key `task.keepout.validate_full` 不存在，从未产出 |
| C3 模型失配敏感性 18 次 | key `nom_scale_mass/drag/thrust` 不存在，全是报错桩 |
| fig5 计算延迟 | 测的是 Isaac rollout fps，K=32→256 数值不变，测量无效，已删图 |
| hard 场景 | 饱和区，无区分度 |
| 内化 C（r1d/r1e） | 不属于本方法，全链路剔除 |

## 6. 本次修掉的实验设计缺陷

1. **Predictive+binary 触发边界** 0.3 → 0.6，与 `soft_hi` 对齐（原先两格差两个变量）
2. **几何门控观测维度** 44 → 45（`risk.gate` 补丁解耦「A 进观测」与「A 做门控」）
3. **Welch 检验** 从正态近似改为真 t 分布
4. **fig3b** 从 `cum_violation`（依赖记录点数的怪指标）改为训练期违约率 + Wilson CI
5. 新增 `check_overrides.py`：命令行同键多值即中止（两次事故都属这一类）

## 7. 明天的排序建议

1. **重标定 hard 场景**（约 1.5 h）——扫 `intercept_steps`，把 PPO 碰撞率调到 **0.2~0.4**。
   这是唯一能同时救活评测期与训练期统计功效的动作。**不做这个，后面都是白跑。**
2. **修 PPO 训练目标**（表述 A）+ 全量重训（约 11 h）。排在 1 之后，因为在没有区分度的
   场景里重训，结果照样埋在噪声里。
3. 补 PPO baseline 的多 seed；修 3 个 config key 让 N1/C3 能跑；
   重做延迟测量（在 `mppi_filter.py` 里打 CUDA-synced 计时，报「单次滤波 ms × 激活率」）。

## 8. 论文定位的现实评估

原定的 headline「预测式安全滤波降低碰撞」**当前数据不支持**，六组对比全部打平。

能站住的最强表述是：

> 在同等安全水平下，比例接管以更长绕行为代价换取显著更平滑的执行与更低的跟踪误差；
> 预测式门控则在两种接管律下都显著减少绕行。

但要注意：**A-only 那一格显示滤波器本身没挣到位置**（安全打平、任务更差）。
在把场景标定到有安全压力之前，很难论证组件 B 的必要性。

**先做第 7 节第 1 条，再决定论文怎么写。**
