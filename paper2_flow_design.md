# 论文② 设计文档:洋流扰动下的 keep-in 管道安全滤波

> 分支 `flow-safety`,工作目录 `/home/jovyan/MarineGym-flow`。
> 与论文①(`method_absoft_pipeline_and_story.md`,keep-out 避障 / RP-PSF)物理隔离。
> 状态:**设计稿,尚未写代码**。2026-08-25。

---

## 0. 一句话方法

> 纯 tracking,无障碍。参考轨迹周围有一条半径 R 的**管道(keep-in tube)**;未建模洋流把载具往管壁上推。
> 一个**在线扰动观测器**估出等效外力 `d̂`;把 `d̂` 注入名义模型,前滚 H 步得到**预测管壁裕度** `m̂`;
> 由 `m̂` 生成连续接管系数 `λ∈[0,1]`;同一个带 `d̂` 的模型用 MPPI 采样出 `u_safe`;
> 实际执行 **`u = (1−λ)·u_rl + λ·u_safe`**。

**候选命名**:DA-PSF(Disturbance-Aware Predictive Safety Filter)/ Tube-PSF。

---

## 1. 最关键的一步重构:把"脱轨"变成硬安全约束

这是整篇能不能立住的地方。直接拿 tracking error 当 risk 是**不行**的(见 §9 已否决方案)。正确做法是把它形式化成一个**keep-in 约束**。

### ⚠️ 已修正:走廊必须是世界系的,不能以参考点为中心

初稿写的是以参考点为中心的管道 `d = R − ‖p − p_ref(t)‖`。**这个写法没有真正解决问题** —— 它是跟踪误差的单调函数,门控信号和奖励主项仍然是同一个量,§9 否决 tracking-error-as-risk 的第一条理由原样成立。

改成**世界系走廊(海沟侧壁)**:

```
corridor:   d(p) = W − |p_axis − c_axis| − r_v      # 到最近侧壁的裕度，W 是走廊半宽
safe set:   𝒞 = { d ≥ 0 }
violation:  d < 0                                    # 二值、可数、可报率
```

关键在于 **`d` 与跟踪误差 `‖p − p_ref‖` 是两个不同的量**:参考线本身在 lemniscate 的两端就贴近侧壁,载具可以跟得很准却仍然贴壁,也可以跟得一般却远离侧壁。门控信号与奖励主项这才真正解耦。

物理场景:海沟/峡谷穿越、结构物之间通行、冰下沿测线航行 —— 横向空间物理受限,而航线本身贴着一侧走。

**额外好处**:走廊最窄的相对位置正好是 lemniscate 的曲率极大点,也就是载具正在换向、横向推力裕度最低的时刻 —— 与 §5 阵风瞄准的设计天然叠加。

### 实现口径

- 半宽用**相对定义**:`W = max|ref_axis| + margin`(margin 默认 0.35 m)。因为 `traj_scale` 每个 episode 随机,绝对半宽会让难度飘;相对定义保证参考线在最贴壁处恰好留 margin 的余量,难度一致。
- 走廊受限轴取 **y**(`corridor.axis: 1`):lemniscate 在 y 上幅度最小(局部 ±0.33 × scale),最贴壁。
- 必须 `freeze_traj_yaw: true` 关掉轨迹绕 z 的随机旋转,否则"侧壁"相对轨迹的位置每个 episode 都不同,难度不可控。

### 指标口径(直接沿用论文①踩过的坑)

- 侧壁**不终止 episode**,只记违约(和论文①"禁区不是物理碰撞体"同一手法)——保证所有方法**曝光量相同**,违约率可比。
- 因此必须把 `Track.yaml` 的 `reset_thres: 0.5` 抬高(实现里设 3.0),否则一飘出去就 reset,曝光不等、指标不可比。
- `wall_viol_frac` 累的是触壁**步数**,episode 末尾除以实际步数做曝光归一化。

---

## 2. 问题设定

- 平台/仿真与论文①一致:MarineGym / Isaac Sim,**BlueROV**(T200×6,`num_rotors == action_dim`),`dt=0.016`,62.5 Hz,`max_episode_length=600`(9.6 s)。
- 任务:Lemniscate 跟踪,`traj_scale_mult=1.0`(原始尺度,不需要绕行空间了)。
- 动力学:Fossen 型 + T200 迟滞。洋流通过 `apply_hydrodynamic_forces` 里的**相对速度** `v_rel = v_b − R(q)ᵀ v_flow` 进入阻尼项 —— 即洋流是通过改变水动力而非直接加力起作用的,这点写方法时要说清。
- 策略:**纯 PPO,单标量奖励**。管壁惩罚 `− w_tube·max(0, −d)²` 进 reward(和论文①一样,baseline 不能是"不知道有管道"的弱 baseline)。

---

## 3. 与论文①的差异化(salami 风险的正面处理)

审稿人/编辑很可能把两篇看成一篇拆二。必须主动把差异写在 contribution 里:

| | 论文① | 论文② |
|---|---|---|
| 约束类别 | keep-out(避开外部障碍) | **keep-in(留在管道内)** |
| 失败机理 | 对手瞄准你的未来位置 | **未建模扰动 + 推力饱和导致不可逆漂移** |
| 名义模型 | 假定准确(<2% 误差) | **模型是错的,必须在线估扰动** |
| 新技术组件 | λ 比例接管 | **扰动观测器进预测门控**(本篇 headline) |
| 泛化故事 | 障碍速度 OOD | **流场强度/时变性/空间结构 OOD** |

**论文②的一号贡献不应该是 λ 软混合(那是论文①的)**,而应该是:
> **把在线扰动估计放进预测门控里** —— 当名义模型本身因未建模水动力而失效时,安全滤波器如何还能给出可信的风险预测。

配套消融:`d̂ = 0`(退化成论文①的假设) vs `d̂` 在线估计。这一格是本篇独有的。

**建议投稿去向**:IEEE JOE(Journal of Oceanic Engineering)或 RA-L;和论文①的 TTE 错开。

---

## 4. Pipeline

```
                          obs_t (含 risk_{t−1} 标量)
                                    │
                                    ▼
                          ┌──────────────────┐
                          │  PPO 策略 π_θ     │
                          └────────┬─────────┘
                                   │ u_rl
   ┌───────────────────────────────┼───────────────────────────────┐
   │                               │                               │
   ▼                               │                               ▼
┌─────────────────────┐            │            ┌──────────────────────────┐
│ A0: 扰动观测器       │            │            │ B: MPPI (带 d̂)           │
│ d̂_t ← 残差广义力     │──── d̂ ────┼──── d̂ ───▶│ K=128 采样, N=20 步       │
│ 低通滤波             │            │            │ cost = 管壁 + 跟踪 + 能耗 │
└──────────┬──────────┘            │            └────────────┬─────────────┘
           │                       ▼                         │ u_safe
           │            ┌──────────────────────┐             │
           └───────────▶│ A: 风险监视器         │             │
                        │ 名义模型 + d̂,ZOH u_rl│             │
                        │ 前滚 H=15 步          │             │
                        │ m̂ = min_k d(p̂_k, t+k)│             │
                        └──────────┬───────────┘             │
                                   │ λ = clamp((h_hi−m̂)/(h_hi−h_lo),0,1)
                                   ▼                         │
                        ┌──────────────────────────────────────┐
                        │  u = (1−λ)·u_rl + λ·u_safe            │
                        └──────────────────────────────────────┘
```

### A0 扰动观测器(本篇新增)

用**残差广义力**,不去反解流速(反解要过阻尼模型的非线性,病态):

```
d̂_raw,t = M_RB·(v_b,t − v_b,t−1)/dt  −  f_nominal(x_{t−1}, u_{t−1})
d̂_t     = (1−α)·d̂_{t−1} + α·d̂_raw,t          # 一阶低通,α≈0.1(≈0.26 s 时间常数)
```

预测时**零阶保持** `d̂` 到整个 H 步 —— 这就是 offset-free MPC 的标准做法,对"缓变流 + 阵风持续时间 > H·dt"成立,写方法时要给出这个适用条件。

注:`nominal_dynamics.py` 里已经有 `f_nominal` 的全部结构(推力 + 附加质量 + 阻尼 + Coriolis + 浮力),`M_RB` 积分那套坑论文①已经踩平(浮力帧翻转 `buoy[[1,2,4,5]]*=-1`、用 `M_RB` 不是 `M_RB+M_A`),直接复用。

### A 风险监视器

和论文①**同一份代码**,只换 clearance 的定义:

```
p̂_0 = p_t;  对 k=1..H:  x̂_k = f_nominal(x̂_{k−1}, u_rl, d̂)     # u_rl 零阶保持
m̂ = min_{k=1..H} [ R − ‖p̂_k − p_ref(t+k)‖ ]
λ = clamp( (h_hi − m̂) / (h_hi − h_lo), 0, 1 )
```

`p_ref(t+k)` 是解析可得的(lemniscate 闭式),不需要额外状态。H=15 → 0.24 s 前瞻,与论文①一致。

### B MPPI

论文①的 `mppi_filter.py` 去掉 `w_coll` 那项,换成管壁代价:

```
cost = w_tube·Σ_k max(0, −d(p̂_k, t+k))²  +  w_track·Σ_k ‖p̂_k − p_ref(t+k)‖²  +  w_eff·Σ_k ‖u_k‖²
```

滚动时同样注入 `d̂`。K=128、N=20、σ=0.4、temperature=0.05 先沿用论文①的冻结值。

---

## 5. 场景设计:阵风必须"瞄准最低控制权限的时刻"

这是**最重要的场景设计决策**,直接对应论文①"静态障碍被 PPO 打穿 → 改成主动拦截"的教训。

**恒定均匀流几乎肯定不够。** 恒定流对策略而言只是一个稳态偏置,PPO 训几十轮就学会顶着流飞了(和静态障碍一模一样的失败模式)。必须让扰动**快到策略来不及适应**,且**打在最脆弱的时刻**。

### 阵风事件(gust)

- 流速在 `τ ∈ [0.2, 0.5] s` 内从 0 斜坡升到 `v_c`,保持一段后衰减
- 方向:**与参考速度方向正交的水平分量**(顺流/逆流只影响速度跟踪,横流才把你推出管壁)
- **发射时刻瞄准 lemniscate 的高曲率点** —— 载具在那里正在减速转向,推力裕度最小,横向权限最低。这是"aim at the future"机制在本篇的对应物:
  ```
  t_gust = argmin over upcoming window of  ( 可用横向推力裕度 )
         ≈ 参考轨迹曲率 κ(t) 的局部极大点
  ```
- 每 `re_gust_period` 步(比如 200 步 = 3.2 s)重新发射一次,形成持续压力

**这个设计的论文意义**:反应式门控(看当前管壁裕度)在阵风上升沿还没反应过来,推力就已经饱和了 —— 到那时是不可逆的。这把"必须预测"从口号变成可测的实验事实,和论文①的 B-only ≈ PPO 是同一个论证结构。

### 公平性

策略必须**拿得到**它能观测到的东西,否则又是弱 baseline。真实 AUV 上 DVL 能测对水速度,所以给策略:
- 相对水速 `v_b − R(q)ᵀ v_flow`(等价于让它间接感知流),**或**
- 直接给 `d̂`(和滤波器同源,最公平)

我建议给 `d̂`:这样"策略拿到扰动估计但仍然失败,而带预测的滤波器成功"的对比更硬 —— 差别就纯粹落在**预测**上,而不是信息不对称。

---

## 6. 消融矩阵与 baseline

### 主消融(2×2,每格相对 ours 只差一个开关,沿用论文①格式)

|  | 斜坡接管 soft | 阶跃接管 binary |
|---|---|---|
| **预测门控** (m̂) | **DA-PSF (ours)** | Predictive + binary |
| **反应门控** (当前 d) | Reactive + proportional | Reactive + binary |

### 本篇独有的第三轴:扰动估计

| | 说明 |
|---|---|
| `d̂` 在线估计 | ours |
| `d̂ = 0` | 退化成论文①的完美模型假设 —— 隔离观测器的贡献 |
| `d̂` = oracle(仿真真值流速) | 上界,量化观测器的损失 |

### Baseline(按危险程度排序)

1. **⚠️ DR-PPO(域随机化):训练时就把强流/阵风全部随机化进去。**
   这是**最危险的审稿意见**("你为什么不直接训?")。必须跑,且必须证明:
   (a) DR 覆盖范围内 DR-PPO 可能确实好 → 那就把 OOD 推到 DR 范围之外;
   (b) DR 的代价是无流工况下的 nominal RMSE 变差(通用性 vs 精度的经典 trade-off)。
   **如果 DR-PPO 在 OOD 上也不失败,本篇没有立足点。** 优先级最高。
2. **纯 MPC(λ≡1)**:全程 MPPI。见 §7 K1,这是另一个生死线。
3. **PPO(无滤波)**:下界。
4. **A-only**:risk 进观测但不接管 —— 论文①已证明它有用(策略自己学会规避),这里同样要有。
5. **经典自适应/扰动抑制**:L1 自适应 或 INDI。审稿人一定会问"和自适应控制比呢"。至少要在 related work 里正面回应,能跑一个更好。

---

## 7. 三个 kill-switch 实验(先跑,便宜,决定生死)

这是从论文①"单步 shield 不 work → 及时转向"学到的纪律。**每一个失败就意味着整篇要改方向,别在实现完整 pipeline 之后才发现。**

### K1(最优先):MPPI 单独的 nominal 跟踪精度,是否显著差于 PPO?

- 做法:无流工况,λ≡1(纯 MPPI)vs 纯 PPO,比 RMSE。
- **若 MPPI 不差甚至更好 → 整篇死**,结论会变成"直接用 MPC 别用 RL",λ 混合失去意义。
- 论文①里 MPPI 的跟踪能力弱于 PPO,预期这里也成立,但**必须先验证**,因为这里 MPPI 的代价函数换成了纯跟踪(不再有 `w_coll` 拖累它),它会比论文①里强很多。
- 成本:两次 eval-only,半天。

### K2:纯 PPO 在设计的阵风场景下,违约率是否显著 > 0?

- 若 ≈ 0 → 场景太容易(静态障碍的翻版),加大 `v_c` / 缩短 `τ` / 改进瞄准,直到 PPO 明显失败为止。
- 同时要确认违约不是"永远失败"(全 100% 也没有区分空间)。目标区间:PPO 违约率 **15–50%**。

### K3:反应式门控是否 ≈ PPO?

- 这是本篇核心论点("必须预测")的直接检验,和论文①的 B-only ≈ PPO 同构。
- 若反应式就够好 → 预测性没有卖点,得回去把阵风上升沿做得更陡(让反应式来不及)。

### K4:扰动观测器是否真的改善了预测?

- 直接量:`d̂=0` vs `d̂` 估计 vs oracle,比 **H 步开环预测位置误差**。
- 这个不涉及策略和滤波器,纯开环验证,最便宜,可以最先做。
- 若 `d̂` 带来的预测改善很小 → 本篇的 headline 贡献不成立,要重新想。

**建议执行顺序:K4 → K1 → K2 → K3。** K4/K1 不需要训练新策略,拿论文①现成的 BlueROV ckpt 就能跑。

---

## 8. 指标

| 指标 | 说明 |
|---|---|
| **管壁违约率**(headline) | 曝光归一化,不终止 episode |
| 累计违约深度 | `Σ max(0,−d)·dt`,比二值率更细 |
| 首次违约时间 / 生存曲线 | 阵风到达后撑多久 |
| **无流 nominal RMSE** | 滤波器的代价,必须报,否则审稿人认为你在拿精度换安全 |
| 最大跟踪误差分布 | 尾部行为 |
| **接管占空比** `P(λ>0.05)`、`mean λ` | 计算成本故事:嵌入式上 MPPI 不能全程跑 |
| 恢复时间 | 阵风结束后回到管中心的时间 |
| 能耗 / 推力饱和占比 | 饱和占比能直接支撑"不可逆"的论证 |

---

## 9. 已否决的方案(存档,别回头再走)

**直接拿 tracking error 当 risk、MPPI 按 tracking error 重选** —— 2026-08-25 否决,三条理由:

1. tracking error **就是** reward 主项,门控信号与优化目标未解耦,是同义反复("策略干得不好时就少信策略")。
2. MPPI 用 <2% 精确名义模型、K=128、N=20,本身就是一个强 tracking MPC。消融会单调显示 λ 越大越好 → 结论变成"别用 RL",方法自我否定。
3. 没有二值违约事件,指标塌成 RMSE,直接撞 residual RL / RL+MPC blending / MPC-guided policy 这一大片拥挤的 baseline 面。

修复方式就是 §1:引入 keep-in 管道,把连续的性能退化重新形式化成二值的硬约束违约。

---

## 10. 审稿人攻击面

| 攻击 | 回应 |
|---|---|
| "直接域随机化训不就行了" | §6 baseline 1,必须有数据。核心答案:DR 换来的是 nominal 精度损失,且 OOD 不可能穷举 |
| "这不就是带切换律的 MPC" | λ 连续、门控在预测裕度上、占空比低(报数字);且 RL 提供 MPC 给不了的 nominal 精度 |
| "脱轨不算 safety" | §1 keep-in 管道 + 三个物理场景(巡检 standoff / 海沟 / 对接走廊) |
| "和自适应控制(L1/INDI)比呢" | §6 baseline 5,至少 related work 正面回应 |
| "和你上一篇是同一个方法" | §3 差异化表;把观测器进门控立为本篇一号贡献 |
| "`d̂` 零阶保持对时变阵风不成立" | 承认,给出适用条件(阵风持续 > H·dt=0.24 s),并用 oracle-`d̂` 消融量化这个近似的损失 |
| "只有一个 seed" | 论文①的老问题。本篇建议 headline 至少 3 seed |

---

## 10.5 实施状态（2026-08-25 夜）

### 环境验证

| 检查 | 结果 |
|---|---|
| 复现噪声底 | **0** —— 环境逐位确定性，V0 因此用严格相等判据 |
| V0 回归（走廊/阵风/安全栈全关） | **PASS**，`pos/vel/reward` 的 `max\|Δ\|` 全为 0 |
| V1 洋流驱动载具 | PASS |
| V2 流向/帧 | PASS，`cos(v_ss, v_flow) = 1.000` |
| V3 幅值 | 原始判据 FAIL(0.708) → 查明为**未收敛**：漂移时间常数 τ≈25 s，400 步不够。渐近拟合 **r∞ = 0.996** → 物理正确。已加拟合判据 |
| 全栈冒烟 | PASS：λ max 0.55、λ>0.05 占 2.6%、滤波修正非零占 5.8%、‖d̂‖ 中位 0.53 N / p95 60 N、无 NaN |

### 标定修正（重要）

初版 `W = max|ref| + margin`，导致完美跟踪的载具在最贴壁处只剩 `margin − r_v = 0.05 m`，任务退化、门控常开。改为

```
W = max|ref_axis| + vehicle_radius + margin      # margin = 完美跟踪者保留的裕度
margin = 0.8 > soft_hi = 0.6                     # 否则跟踪完美时 λ 也永远 >0
```

### 实验设计：一条策略 + 多个滤波器

主消融矩阵的所有滤波器格子**共用同一条已训练策略**，只在评测期换开关；前提是 `safety.risk.in_obs=false`，使各格观测维度一致。这比论文①"每格各训一条"更受控（差异纯粹来自滤波逻辑），且在 GPU 仅剩 ~5 GB 时可行。risk 进观测作为独立的一条轴，单独训 `riskobs` 策略比。

### 已知性能约束

MPPI(K=128,N=20) 实测 **2.4 s/步 @16 envs**。已修一处结构性遗漏：软接管路径漏了整批早退（`active_mask` 全 false 时跳过 K×N 前滚）。仍不够则降到 K=64,N=10。另：GPU 被其他租户占满，非本工程可控。

### 顺带发现的上游实现问题

- `underwaterVehicle.py:324` `flow_vels = rand_like(...) * max_flow_vel` → 均匀 `[0,max]` 且符号跟随配置，**均值只有配置值的一半**，无法做跨 episode 的方向随机化（阵风改成逐步写入后绕开了）
- `underwaterVehicle.py:185` 噪声用 `rand_like`(均匀[0,1]) 而非 `randn` → **均值非零**，是正偏置而不是零均值噪声

---

## 11. 实施顺序与代码改动清单

> 全部在 `flow-safety` 分支。共用基础设施的改动先在 `main` 上提再 rebase 过来。

### Step 0 — 开环验证(不训练,最便宜)
- [ ] K4:`d̂` 观测器 + H 步预测误差对比。可以挂在论文①的 `validate_dynamics` 钩子上扩展。

### Step 1 — 洋流场景
现有 `disturbances.yaml` + `underwaterVehicle.set_flow_velocities` 只能做**每 episode 恒定 + 逐步噪声**的流,不够用。需要:
- [ ] 支持**逐步写入** `drone.flow_vels`(现在只在 `_reset_idx` 写一次,`track.py:392`)
- [ ] 阵风事件生成器:斜坡 τ、幅值 `v_c`、正交方向、瞄准高曲率点、周期重发
- [ ] 训练档 / 评测档分离(`disturbances.yaml` 已有 `train`/`evaluate` 两档,`mode` 选择,直接用)

**⚠️ 现有实现的两个坑(读代码时发现,写场景前要处理)**:
1. `underwaterVehicle.py:324` `flow_vels = torch.rand_like(...) * max_flow_vel` → **均匀 [0, max],恒正**,流向永远在 +x+y+z 卦限,不是各向同性。要改成有符号采样。
2. `underwaterVehicle.py:185` 噪声用 `torch.rand_like`(均匀 [0,1])而非 `randn` → **噪声均值非零**,是个正偏置而不是零均值噪声。

### Step 2 — keep-in 管道约束
- [ ] `Track.yaml` 加 `tube:` 段(`radius`、`reward_weight`、`reward_margin`、`in_obs`)
- [ ] `track.py`:管壁 clearance、违约 stats、曝光归一化、抬高 `reset_thres` 使违约不终止
- [ ] K2:纯 PPO 基线,确认违约率落在 15–50%

### Step 3 — 复用 A/λ/B
- [ ] `risk_monitor.py`:clearance 抽象成 `source: keepout | tube`;注入 `d̂`
- [ ] `mppi_filter.py`:`w_coll` → `w_tube`;注入 `d̂`
- [ ] K1、K3

### Step 4 — 完整消融 + baseline
- [ ] 2×2 主矩阵 + 扰动估计三档 + 5 个 baseline,3 seed

**GPU 现实**:4× L40 但每张已被其他租户占 40+ GB,同时两个训练大概率 OOM。长训练串行,`nvidia-smi` 先看卡。

---

## 12. 与论文①共享的资产

- `nominal_dynamics.py`:一步误差 <2%,坑已踩平,直接用
- `mppi_filter.py` / `risk_monitor.py`:结构复用,只换 clearance 定义 + 注入 `d̂`
- BlueROV ckpt(`scripts/outputs_aei/`,在主树,用绝对路径引用,别复制)
- 报表脚本 `scripts/aei_report.py` 的汇总/显著性检验流程
