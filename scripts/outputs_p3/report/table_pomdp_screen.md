# POMDP 机制筛选：AUV 轨迹跟踪在什么条件下才需要历史？

策略固定为**单帧 MLP**（无历史）。逐一施加部分可观测机制，看 `episode_len`
相对**同任务**无 POMDP 基准掉多少。掉得越多 ⇒ 信息缺失越致命 ⇒
序列模型的补偿空间越大。只有明显掉下去的条件，才值得用来比较序列架构。

## 任务设定：slowed reference (trackable)（基准 episode_len = 216.2）

| 部分可观测机制 | episode_len ↑ | 相对基准 | Return ↑ | 判定 |
|---|---|---:|---|---|
| `s2_noff_v` — …and no DVL/gyro | 213.9 ± 0.88 | -1.1% | 181.7 | 无效（策略无需历史） |
| `s2_noff` — No future waypoints, no time encoding | 215.3 ± 0.67 | -0.4% | 182.8 | 无效（策略无需历史） |
| `s2_sparse` — USBL fix every 10 steps (6.2 Hz) | 215.6 ± 1.04 | -0.3% | 183.7 | 无效（策略无需历史） |
| `s2_novel` — No DVL, no rate gyro | 215.6 ± 0.83 | -0.2% | 183.5 | 无效（策略无需历史） |
| `s2` — — (no degradation, baseline) | 216.2 ± 0.75 | +0.0% | 184.5 | 基准 |

## 任务设定：original reference speed (capability-limited)（基准 episode_len = 57.9）

| 部分可观测机制 | episode_len ↑ | 相对基准 | Return ↑ | 判定 |
|---|---|---:|---|---|
| `drift` — Unobserved thruster-gain drift  U(0.5,1.0) | 57.9 ± 0.14 | +0.0% | 49.3 | 基准 |

## 结论

> 误差棒是跨评测 seed 的 std（约 ±0.5%），故 |相对基准| < 2% 视为无差异。

**只要目标位置持续可见，「追当前目标点」就是无记忆最优策略。**
这解释了为何屏蔽速度、加观测延迟、稀疏定位、切断前馈参考、执行器增益漂移
等机制都测不出差异 —— 它们只是让策略「看得少一点」，而比例控制依然够用。

真正需要历史的条件必须满足其一：

1. **目标位置不再持续可见**（真实 USBL 速率 0.5–1 Hz + 无 DVL ⇒ 必须航位推算）；
2. **存在时变、有相位的外部扰动**（涌浪诱导力 ⇒ 反应式控制永远滞后半个周期，
   要抵消必须从历史估计 (A, ω, φ) 做前馈 —— 内模原理）。

> 另外必须注意：在**原始参考速度**下任务是**能力受限**的
> （载具只跑出参考速度的 51%，episode 长度由运动学锁死在 ~58 步），
> 此时任何机制都测不出差异。比较策略架构前必须先把参考速度降到载具能力之内。
