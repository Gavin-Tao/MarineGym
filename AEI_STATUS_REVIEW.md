# AEI 实验复盘（数据截至 2026-08-25 07:03，报告于 08-25 重整）

## 0. 报告范围

**方法**：RP-PSF = A(RiskMonitor 风险门控, H=15) → λ → B(MPPI Exact, K=128/N=20) → soft blend。

只报告两件事：**本方法 vs PPO baseline**、**本方法自身的消融**（门控信号 × 接管律 2×2 + A-only）。

"内化 C"（`internalize_weight` / `internalize_adaptive`）**不属于本方法**，已从 `collect_aei.py`、
`aei_report.py`、全部 CSV、全部图、`RESULTS.md` 中彻底移除。相关的两次训练（`r1d_fixed_C`、
`r1e_adaptive_C`）及其评测数据不再出现在任何产出里。

## 1. 跑完情况

三条 driver 全部正常收尾：

| 阶段 | 时间 | 状态 |
|---|---|---|
| 消融训练（本方法 4 个变体，seed=0，120 iters） | 19:02 → 00:45 | rc=0 |
| 评测（nominal/ood 各 10 evaluation seed，hard 5 个） | 19:15 → 06:49 | 全 rc=0 |
| 补训练 seed（RP-PSF / Predictive+binary × seed 1,2） | 01:46 → 06:28 | 4/4 rc=0 |
| 收尾 + 出图 | 06:49 → 07:03 | 见第 4 节 |

## 2. 主结果：评测期打平，但**这是分辨率问题，不是方法无效**

| scene | PPO | ours | p |
|---|---|---|---|
| nominal | 0.0078 | 0.0084 | 0.86 |
| ood | 0.0164 | 0.0169 | 0.90 |
| hard | 0.7476 | 0.7487 | 0.95 |

关键是**检验功效**（`data/power.csv`）：

| scene | PPO 碰撞率 | 每组 episodes | 80% 功效下可检出的最小相对下降 |
|---|---|---|---|
| nominal | 0.78% | 1405 | **89%** |
| ood | 1.64% | 1418 | **67%** |
| hard | 74.8% | 402 | 12% |

在 0.78% 的基线上，现有样本量只能检出「碰撞率下降 89% 以上」这种量级的效应。
**"p=0.86" 只说明这个评测看不见，不说明方法没用。**

代价侧：detour_ratio 在 nominal 显著升高（p=8e-4），ood（p=0.064）与 hard（p=0.019）。
（注：Welch 已从正态近似改为真 t 分布，小样本下更保守；ood 因此由"显著"退回"不显著"。）

`hard` 场景两臂碰撞率均约 0.75、min_clearance 为负，处于**饱和区**，标定失败，暂不具备区分度。

## 3. 训练期安全：本方法最强的一条，且图很干净

`fig4_training_violations`：RP-PSF 的累计违约曲线**保持在 0 直到第 ~74 迭代**，是所有变体里最久的；
PPO baseline 从第 20 迭代起持续攀升，终值 0.400。

但 `fig3b` 的**排序不能用**——把补的 seed 接上后：

| 变体 | 训练 seed 数 | cum_violation |
|---|---|---|
| RP-PSF (ours) | 3 | 0.189 ± 0.029 (0.159 / 0.228 / 0.180) |
| Predictive + binary | 3 | 0.169 ± 0.095 (0.056 / 0.288 / 0.164) |
| Geometric + prop. | 1 | 0.118 |
| A-only | 1 | 0.166 |
| Geometric + binary | 1 | 0.229 |
| **PPO baseline** | **1** | **0.400** |

Predictive+binary 单 seed 的 0.056 是运气：它自己的 seed 跨度 0.056~0.288，几乎覆盖全部变体的排序跨度。
**消融之间互相区分不开**；但 PPO 的 0.400 落在所有变体之外，这条差距**可能是真的**——只是 PPO 只有 1 个 seed，做不了检验。

**根因**：整个 120 迭代训练里 PPO 一共只碰了约 7 个 episode、RP-PSF 约 3 个。这是泊松噪声量级。

## 4. 收尾三项：2 项全空，1 项无效（已从产出移除）

| 项 | 状态 | 原因 |
|---|---|---|
| N1 名义模型单步误差 | **失败** | `task.keepout.validate_full` 这个 config key 不存在，`nominal_onestep.json` 从未生成 |
| C3 模型失配敏感性（18 次） | **全失败** | `nom_scale_mass/drag/thrust` 三个 key 都不存在，18 个 log 全是 823 字节报错桩 |
| fig5 计算延迟 | **测量无效，已删图** | 测的是 Isaac rollout fps，`ms/step` 在 K=32→256 上完全不变，测不到 MPPI 开销 |

`aei_finalize.sh` 只 `say "完成"` 而不检查有没有真结果，所以 `RESULTS.md` 曾看起来是完整的。

## 5. 为什么七月看着好、现在不行

七月 §11.3 那张表（PPO coll 0.023 / ours 0.000）和八月的评测**测的不是同一个东西**：

1. **口径**：七月是**训练期窗口均值**（随机策略、on-policy 采样）；八月是**独立评测**（`ExplorationType.MODE`，确定性策略）。
2. **机制**：滤波器的价值集中在**探索噪声**上。训练时策略在采样、会主动撞出危险动作 → 触发率 6~11%，滤波器有事可做；
   评测时用确定性策略，PPO 自己的碰撞率从训练均值 0.0167 掉到 0.0078，滤波器触发率也掉到 1.7~3%。**没有探索噪声，就没有需要拦的东西。**
3. **样本量**：七月那个 "0.000" 是 ~200 个 episode 零碰撞，95% 置信上界约 1.5%，本来就盖住了 PPO 评测的 0.78%——**从来没有显著过**，只是当时没做检验。

## 6. 下一步（让效应真的能被看见）

1. **重标定 hard 场景**：扫 `intercept_steps`，把 PPO 碰撞率调到 **0.2~0.4**（现在 0.75 是饱和，[25,55] 压太狠）。
   那个区间每组只要约 121 个 episode 就能检出 50% 的下降，而现有每组有 1400——功效富余 10 倍。
2. **补 PPO 的 seed 1、2**：训练期违约就有 n=3 vs n=3，可以做检验。这是目前最像真结果的一条。
3. **评测那 4 个已训好的 checkpoint**：把统计从"评测 seed"改成"训练 seed"，去掉伪重复。
4. 修 3 个 config key（`validate_full` / `nom_scale_*`），N1 与 C3 才能重跑。
5. 重做延迟测量：在 `mppi_filter.py` 里对滤波调用打 CUDA-synced 计时，报"单次滤波 ms × 激活率 = 有效开销"。
