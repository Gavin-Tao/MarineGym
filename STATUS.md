# 论文② 进度与计划

> 更新时间：2026-08-26 07:10。分支 `flow-safety`，工作目录 `/home/jovyan/MarineGym-flow`。
> 方法与设计见 `paper2_flow_design.md`；观测器判决见 `scripts/outputs_flow/K4_FINDINGS.md`。

---

## ⛔ 当前阻塞项（最高优先级）

**同一份 checkpoint，训练时报的指标与评测差 7–14 倍。**

| | ep_len | 跟踪 RMSE | min 侧壁裕度 | 违约率 |
|---|---|---|---|---|
| 训练日志（iter 491，**开阵风**） | 594 | **0.158** | 0.757 | 0.015 |
| 评测 nominal（同配置，开阵风） | 500 | **1.183** | 0.398 | 0.213 |
| 评测 calm（**关阵风**） | 576 | 0.341 | 0.721 | 0.053 |
| 评测 **随机策略**（不加载 ckpt） | 541 | 1.970 | 0.251 | 0.256 |

最刺眼的一条：**训好的策略在评测里只比随机策略好 40%**（1.183 vs 1.970）。
一条训了 410 万帧的策略不该是这个样子。

### 已排除的可能

| 嫌疑 | 排除依据 |
|---|---|
| 训练后环境代码被改动 | `git diff` 显示只有阵风 RNG（分布相同）与 MPPI 采样中心（只影响 `mpc_only`） |
| 观测归一化器未随 ckpt 保存 | transform 栈只有 `InitTracker`，无归一化；`ValueNorm` 只作用于 critic |
| checkpoint 没加载成功 | `train.py` 用 strict 加载，不匹配会直接报错；`ppo.py` 在加载前已物化 LazyLinear |
| 阵风分布变了 | 实测流速幅值仍在 `[0.6,1.0]`、符号双向 |
| `num_envs` / `train_every` 不同 | 用训练时的收集器设置（256/32）评测，结果与 128/64 一致 |
| 域随机化在评测被打开 | `enable_randomization` 只在 `tiltRotor.py` 实现，BlueROV 走的 `underwaterVehicle.py` 根本没用 |
| 确定性 vs 随机策略 | actor 是状态无关方差的高斯，`MODE = loc`（均值网络输出），不该劣于采样 |

### 正在查

沿训练轨迹评测 4 个 checkpoint（iter≈100 / 200 / 415 / 500），与训练当时报出的值逐点对照：

- 早期点对得上、后期对不上 → 训练指标在后期变乐观
- 各点差一个恒定倍数 → 评测路径存在系统性偏差

**在这个搞清楚之前不重跑评测矩阵**，否则可能又是几小时白跑。

---

## ✅ 已完成

### 环境正确性

| 检查 | 结果 |
|---|---|
| 复现噪声底 | **0**，环境逐位确定性 |
| V0 回归（走廊/阵风/安全栈全关） | **PASS**，`pos/vel/reward` 差值全为 0 |
| V1 洋流驱动载具 | PASS |
| V2 流向与坐标系 | PASS，`cos(v_ss, v_flow) = 1.000` |
| V3 幅值标定 | 原判据 FAIL(0.708) → 查明是**未收敛**（τ≈25 s），渐近拟合 **r∞=0.996** |
| 全栈冒烟 | PASS，λ / d̂ / 违约量级正常，无 NaN |
| 逐 env 掩码等价性 | **逐位等价**：λ=0 的 1235 个样本 max\|Δu\| = 0 |

### 实现

- **走廊 keep-in 约束**：世界系侧壁，`W = max|ref| + r_v + margin`，违约不终止、曝光归一化
- **阵风**：梯形波逐步写入 `flow_vels`，峰值对齐参考轨迹极值点（推力裕度最低处），方向由**参考点**决定（外生，各方法面对同一扰动）
- **扰动观测器**：残差广义力 + 一阶低通，可注入名义模型前滚
- **安全栈**：A(预测风险) → λ → B(MPPI 走廊滤波)，消融开关齐全
- **指标面板**（都算在滤波后的实际动作上）：
  `max_intrusion` / `first_viol_step`(生存) / `max_track_err` /
  `action_rate` / **`action_jerk`(抖振，soft vs binary 的机理判据)** /
  `sat_frac` / `effort` / `power_W` / `engage_frac`
- **可比性**：阵风与 MPPI 各有独立 RNG，全局流只被环境使用 → 同一 seed 内各格面对
  完全相同的轨迹与扰动，消融是**配对**比较

### 已产出的论文材料

| 产物 | 内容 |
|---|---|
| `figures/fig6_training_curves` | 违约率 0.37→0.02，跟踪 RMSE 2.5→0.15 |
| `figures/fig7_calibration_sweep` | 违约率 vs 阵风幅值，说明 OOD 档位是量出来的 |
| `figures/fig8_observer_prediction` | K4：三种 d̂ 的预测质量（含负结果） |
| `K4_FINDINGS.md` | 观测器判决 + **零阶保持重复计入扰动**的机理诊断 |
| `PERF_NOTES.md` | 评测成本是 launch-bound 的实测证据 |
| `RESULTS.md` | 自动生成的结果表（含读数须知与统计功效边界） |

### 已通过的 kill-switch

- **K1**（不能直接用 MPC）：无扰动下 MPC-only 跟踪 RMSE **0.927** vs PPO **0.341**（2.7×），
  但 MPC-only 违约率 0.000 vs PPO 0.053 → 安全/精度互补成立，λ 混合有意义
- **K2**（场景标定）：违约率随阵风幅值单调上升、2.6 m/s 后饱和，档位落在可分辨区间
- **K4**（观测器）：强扰动档 MAE 减半（0.080→0.035），门控一致率 0.953→0.986

---

## 📋 待办与预计时间

> 前提：阻塞项解决。若需重新训练策略，整体顺延约 2 小时。
> GPU 现状：4×L40 与另外两个 session + 其他租户共享，空闲显存常只剩 2–3 GB，**必须串行**。

| # | 任务 | 预计耗时 | 说明 |
|---|---|---|---|
| 0 | **定位并修复训练/评测差异** | 1–2 h | 阻塞项；可能需重训（+2 h） |
| 1 | 评测矩阵 8 格 × 4 场景 × 3 seed | ~8 h | 含新指标面板；先出 kill-switch 判决再跑第二批 |
| 2 | 门控边界 `soft_hi` 敏感性扫描 | ~1 h | `ours/calm` 显示门控在无扰动时也开 6.5%，边界需重新标定 |
| 3 | 观测器 α 敏感性扫描 | ~30 min | 把"低通衰减恰好抵消零阶保持偏差"变成有据可查的设计参数 |
| 4 | DR-PPO 基线（训练 + 评测） | ~3 h | 最危险的审稿意见："为什么不直接域随机化训" |
| 5 | 闭环前滚改进版 + K4 复测 | ~2 h | 前滚时重新查询策略，消除重复计入；届时 oracle 应成为最优 |
| 6 | 出终版表格 + 图 + 显著性 | ~30 min | 全自动 |

**乐观路径**（阻塞项当天解决、不用重训）：约 15 小时，即 **今日 22:00 前后**出完整结果。
**保守路径**（需要重训 + soft_hi 重标定后再跑一遍矩阵）：约 26 小时，即 **明日 09:00 前后**。

---

## 已知需要处理的问题（非阻塞）

1. **`ours/calm` 的门控占空比偏高**（6.5%，带来 +14% 标称跟踪代价）。
   原因是标定：margin=0.8 而跟踪 RMSE≈0.34，普通跟踪误差就能把裕度压到 0.46 < soft_hi=0.6。
   由待办 #2 解决。
2. **`mpc_only` 在 calm/nominal 两档都是零违约**，帕累托优势只能靠跟踪精度撑。
   需要在 strong/fast 档确认 MPC 是否也零违约 —— 若是，"为什么不用 MPC"的答案只剩精度与算力。
3. **统计功效**：每格约 80 episode 时只能分辨 ~0.2 量级的违约率差。
   多 seed 评测（用户 2026-08-26 指示：训练只跑一次，方差来自 eval seed）会改善这一点。
4. **待补跑**：`ppo/calm` 缺 `.episodes.csv`（见 `RERUN_TODO.md`）—— 重跑矩阵时自动解决。
