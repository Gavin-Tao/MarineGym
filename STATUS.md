# 论文② 进度与计划

> 更新时间：2026-08-26 09:20。分支 `flow-safety`，工作目录 `/home/jovyan/MarineGym-flow`。
> 方法与设计见 `paper2_flow_design.md`；观测器判决见 `scripts/outputs_flow/K4_FINDINGS.md`。

---

## ✅ 阻塞项已解决（2026-08-26 09:00）

**根因：阵风在第一个 episode 之后从不发射。**

`IsaacEnv` 的 reset 顺序是先 `_reset_idx(env_ids)` 再 `progress_buf[env_ids] = 0`
（`isaac_env.py:275/277`）。阵风发射在 `_reset_idx` 里读 `progress_buf`，拿到的是
**上一 episode 的末值（≈599）**，于是起风时刻与下次重发都被排到新 episode 永远追不上
的未来 —— 整个 episode 一次阵风都不发。只有最初那次 reset（`progress_buf` 本就是 0）正常。

它伪装成"训练/评测差 7 倍"：训练跑 410 万步，绝大多数 episode 在第一次 reset 之后
（**无扰动**）；评测只跑 768 步，大多是**第一个** episode（有扰动）。所以那条策略从未
见过阵风，拿去评测有阵风的场景只比随机策略好 40%。

连带修复：阵风瞄准用的 `_compute_traj` 同样以 `progress_buf` 为基准，过期值会让它瞄到
错误的轨迹相位、方向可能取反。

**验证**：跨 ≥2 次 reset 采样，第 7 个 episode 阵风活跃 65.9%（修复前恒为 0）。
修复后 V0 回归仍逐位通过。

**修复确认**（同一 checkpoint、同配置）：

| | ep_len | 跟踪 RMSE | min 裕度 | 违约率 |
|---|---|---|---|---|
| 训练日志（iter 585） | 590 | 0.480 | 0.729 | 0.023 |
| 评测 nominal | 590 | 0.494 | 0.767 | 0.023 |

四项吻合，差异消失。

**代价**：策略重训（已完成，50 min）；基于旧策略的评测全部作废
（归档在 `scripts/outputs_flow/eval_VOID_pregustfix/`，附作废说明）。

**流程补强**：V 层原本验证了阵风的波形/幅值/方向/瞄准，却没验证它在多次 reset 之后
是否还在发 —— 所有冒烟测试只跑 120–300 步，恰好都在第一个 episode 内。
已加 `diag_gust_persist.sh` 补上这条。

## 场景标定（K2，重训后，全部通过）

| 场景 | 违约率 | 触壁步占比 | min 裕度 | 跟踪 RMSE | 判定 |
|---|---|---|---|---|---|
| calm | 0.023 | 0.000 | 0.812 | 0.275 | 标称代价基线 |
| nominal（训练分布） | 0.023 | 0.023 | 0.767 | 0.494 | ✅ 分布内已安全 |
| strong（OOD 幅值） | 0.385 | 0.316 | 0.014 | 1.734 | ✅ 有改进空间 |
| fast（OOD 幅值+上升沿） | 0.355 | 0.222 | 0.071 | 1.651 | ✅ 有改进空间 |

`strong` 与 `fast` 幅值相同、只差上升沿：违约率接近但 `fast` 的触壁步占比明显更低
（0.222 vs 0.316）—— 陡上升沿冲量更小但更突然，正是"反应式门控来不及"该显现的地方。
K3 的判据落在这一档。

## ✅ 已完成

### 环境正确性

| 检查 | 结果 |
|---|---|
| 复现噪声底 | **0**，环境逐位确定性 |
| V0 回归（走廊/阵风/安全栈全关） | **PASS**，`pos/vel/reward` 差值全为 0 |
| V1 洋流驱动载具 | PASS |
| V2 流向与坐标系 | PASS，`cos(v_ss, v_flow) = 1.000` |
| V3 幅值标定 | 原判据 FAIL(0.708) → 查明是**未收敛**（τ≈25 s），渐近拟合 **r∞=0.996** |
| 全栈冒烟 | PASS，λ / d̂ / 违约量级正常，无 NaN |
| 逐 env 掩码等价性 | **逐位等价**：λ=0 的 1235 个样本 max\|Δu\| = 0 |

### 实现

- **走廊 keep-in 约束**：世界系侧壁，`W = max|ref| + r_v + margin`，违约不终止、曝光归一化
- **阵风**：梯形波逐步写入 `flow_vels`，峰值对齐参考轨迹极值点（推力裕度最低处），方向由**参考点**决定（外生，各方法面对同一扰动）
- **扰动观测器**：残差广义力 + 一阶低通，可注入名义模型前滚
- **安全栈**：A(预测风险) → λ → B(MPPI 走廊滤波)，消融开关齐全
- **指标面板**（都算在滤波后的实际动作上）：
  `max_intrusion` / `first_viol_step`(生存) / `max_track_err` /
  `action_rate` / **`action_jerk`(抖振，soft vs binary 的机理判据)** /
  `sat_frac` / `effort` / `power_W` / `engage_frac`
- **可比性**：阵风与 MPPI 各有独立 RNG，全局流只被环境使用 → 同一 seed 内各格面对
  完全相同的轨迹与扰动，消融是**配对**比较

### 已产出的论文材料

| 产物 | 内容 |
|---|---|
| `figures/fig6_training_curves` | 重训后（阵风生效）：跟踪 RMSE 1.9→0.6 单调；违约率 **非单调** 0.35→0.03→0.09→0.03 —— 走廊让安全与跟踪真正耦合的直接证据 |
| `figures/fig7_calibration_sweep` | 违约率 vs 阵风幅值，说明 OOD 档位是量出来的 |
| `figures/fig8_observer_prediction` | K4：三种 d̂ 的预测质量（含负结果） |
| `K4_FINDINGS.md` | 观测器判决 + **零阶保持重复计入扰动**的机理诊断 |
| `PERF_NOTES.md` | 评测成本是 launch-bound 的实测证据 |
| `RESULTS.md` | 自动生成的结果表（含读数须知与统计功效边界） |

### 已通过的 kill-switch

- **K2**（场景标定）：**已在重训后的策略上复核通过**，见上表
- **K4**（观测器）：强扰动档 MAE 减半（0.080→0.035），门控一致率 0.953→0.986。
  该实验用 300 步单次 reset 采集，全程在第一个 episode 内，**不受阵风 bug 影响**
- **K1**（不能直接用 MPC）：旧策略上 MPC-only 跟踪 2.7× 差于 PPO 且零违约，结构性结论稳固，
  但**数值需在重训后的策略上复核**（headline 批次含 `mpc_only` 四格）
- **K3**（必须预测）：判据落在 `fast` 档，headline 批次给出

---

## 📋 待办与预计时间

> 前提：阻塞项解决。若需重新训练策略，整体顺延约 2 小时。
> GPU 现状：4×L40 与另外两个 session + 其他租户共享，空闲显存常只剩 2–3 GB，**必须串行**。

| # | 任务 | 预计耗时 | 说明 |
|---|---|---|---|
| 0 | ~~定位并修复训练/评测差异~~ | ✅ 完成 | 阵风 reset 计时 bug；策略已重训 |
| 1a | **headline 16 格**（ppo/ours/react_soft/mpc_only × 4 场景） | ~3.5 h | 🔄 进行中，含 K1/K3 判据 |
| 1b | 补完消融矩阵（pred_binary/react_binary/dhat_zero/dhat_oracle × 4） | ~4 h | |
| 2 | 门控边界 `soft_hi` 敏感性扫描 | ~1 h | `ours/calm` 显示门控在无扰动时也开 6.5%，边界需重新标定 |
| 3 | 观测器 α 敏感性扫描 | ~30 min | 把"低通衰减恰好抵消零阶保持偏差"变成有据可查的设计参数 |
| 4 | DR-PPO 基线（训练 + 评测） | ~3 h | 最危险的审稿意见："为什么不直接域随机化训" |
| 5 | 闭环前滚改进版 + K4 复测 | ~2 h | 前滚时重新查询策略，消除重复计入；届时 oracle 应成为最优 |
| 6 | 出终版表格 + 图 + 显著性 | ~30 min | 全自动 |

**当前预计**：headline 约 13:00 出；完整矩阵 + 扫描 + DR 基线约 **明日 02:00 前后**。
多 seed 补方差视范围另计（评测成本是 launch-bound，见 `PERF_NOTES.md`）。

---

## 已知需要处理的问题（非阻塞）

1. **门控边界可能仍需重标定**。旧策略上 `ours/calm` 门控在无扰动时也开 6.5%、带来
   +14% 标称跟踪代价。重训后跟踪 RMSE 降到 0.275（calm），裕度更宽，情况应有改善 ——
   但要等 headline 的 `ours/calm` 确认。由待办 #2 的 soft_hi 扫描定工作点。
2. **`mpc_only` 在 calm/nominal 两档都是零违约**，帕累托优势只能靠跟踪精度撑。
   需要在 strong/fast 档确认 MPC 是否也零违约 —— 若是，"为什么不用 MPC"的答案只剩精度与算力。
3. **统计功效**：每格约 80 episode 时只能分辨 ~0.2 量级的违约率差。
   多 seed 评测（用户 2026-08-26 指示：训练只跑一次，方差来自 eval seed）会改善这一点。
4. ~~`ppo/calm` 缺 `.episodes.csv`~~ —— headline 批次重跑时已解决。
5. **旧数据作废**：`eval_VOID_pregustfix/`（阵风 bug 修复前的 32 个格子）与
   `eval_undertrained/`（欠训练策略）均**不得用于论文**，目录内附有说明。
