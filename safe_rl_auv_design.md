# 新论文设计文档:可行性认证的分层安全强化学习水下航行器控制

> 定位:在已发表的 *Average-Power-Budgeted AUV Control via Constrained RL* 之上,
> **换约束语义(软→硬)、加安全认证层、升立论**,做成一篇以"逐步硬安全保证 + 输入受限下可行性认证"为主线的新论文。

---

## 0. 一句话定位与和前作的区分

- **前作 claim**:用 PPO-Lagrangian 把**平均功率**约束成一个可声明的瓦特预算——这是**软的、期望意义**的约束(`J_c ≤ d` 只在训练收敛后、平均意义上成立)。
- **本文 claim**:把安全从"软的期望约束"升级为**逐步硬保证**——**任意时刻**都不进禁区、不超推力/瞬时功率包络,并在**输入受限 + 有界洋流**下给出**前向不变性证明**(而非名义理想模型下的纸面安全)。能量退成顺带汇报的次要指标。
- **一句话 headline**:*Feasibility-certified, collision-free reinforcement learning for AUV control under actuation and power limits.*
- **model-free 张力的正面拆解(必须写进 intro)**:前作卖点是"不需难辨识水动力参数",而 CBF/预测层要 Fossen 模型——看似矛盾。解法:**精确跟踪由 model-free RL 学;硬安全只用一个保守过近似的安全模型**。CBF 只吃控制方向 `M⁻¹B` 与漂移项 `Cν+Dν_r+g` 的**幅值包络**,不需精确值(高估惯量、低估控制权限 → 更保守但仍安全)。两者需求正交,张力被解成一个清晰贡献,而不是漏洞。
- **与前作的关系**:testbed 复用前作,但约束语义(软期望→逐步硬)、算法结构(加 action inspector + 可行性层)、理论保证(前向不变 + 可行性)全部不同;正文正常引用前作并说明 delta 即可。
- **学习器是纯 PPO(重要,别混淆)**:本文**不用**约束/拉格朗日 PPO、**不设**平均功率软约束、**无**对偶变量 `λ`、**无** cost critic;策略只用任务奖励(+ 可选修正惩罚)优化。**所有硬安全与功率包络交给 action inspector(=安全 shield)+ 预测层**。平均功率仅作汇报指标,不再优化。PPO-Lag 只属于前作。

---

## 1. 总体思路与架构选择

核心是两个职责:**(i) 高层学一个能量友好、抗扰、且尽量已满足安全的策略;(ii) 一个安全层,把策略提议的动作以最小改动投影到"始终可行"的安全集,并给形式化证书。** 关键分工:

> **瞬时投影证书 vs. 可行性认证**——单步 shield 给"此刻怎么修到安全"的证书,但该证书只在一个**控制不变子集 `𝒮 ⊆ 𝒞`** 上非空(输入有界时,冲太快会无解,见 §5.1);**维持系统留在 `𝒮` 内、从而保证 shield 始终可行**,是**预测/backup 层**的职责。这条分工把"为什么要两层"从需要辩护的弱点,变成一句精确的技术命题。

**架构分叉(必须显式权衡后再定,别默认堆栈)**:

| 方案 | 结构 | 优点 | 代价 |
|---|---|---|---|
| **A.(本文)shield + 约束 MPC** | 策略 → HOCBF-shield → 约束 MPC | 预测时域捕捉 CBF 近视、平滑、动态可行 | MPC 递归可行要证 + 实时开销(打破零成本) |
| **C.(本文)shield + backup 策略**(backup-set CBF, Gurriet/Ames) | 策略 → shield(不变集由 backup 流**隐式定义**) | **保住前作"近零推理开销"卖点**、天然解决可行性、更 novel | backup 集构造要推 |
| B. 预测式安全滤波器(Wabersich–Zeilinger) | 策略 → 单个 MPC-based safety filter | 一层同时给可行性+安全证明 | 与 A 高度重叠;**仅作相关工作提及,不实现** |

**A 与 C 并列实现,做 head-to-head 对比**——两者共享同一 shield,只在"预测/可行性层"用不同机制,且**共享同一训练好的策略**(训练期只挂廉价的 shield + backup,见 §3),仅在**部署期**分叉:C 用 backup 策略、A 在 shield 后再加约束 MPC。这个对比本身是贡献:在同一策略、同一 shield 下,量化"预测式安全(MPC)"相对"廉价 backup 滤波"的收益(近视违约↓、smoothness↑)与代价(实时开销、打破近零推理成本)。

- **C 的独立价值**:backup 前向积分**隐式定义控制不变集** `𝒮`,shield 只需保证"存在把状态导回 backup 流的动作",在线开销≈一次前向积分/查表,**保住前作近零推理开销**、天然解决输入受限可行性(§5.1)。
- **A 的独立价值**:MPC 预测时域捕捉单步 CBF 的**近视**(当下安全、N 步后必违约),直接改善 smoothness、保证动态可行;代价是实时开销——**正是要实测的量**。
- 方案 B 与 A 高度重叠,仅作相关工作提及,不实现。

testbed **完全复用前作**:MarineGym、三台机器人(BlueROV / BlueROV-Heavy / CUREE-AUV)、四个任务(Hover / Track-Lemniscate / Track-Circle / Track-Spiral),**唯一改动是给每个任务加 keep-out 禁区**(球形碰撞体,近乎零成本),把安全从"执行器约束过滤"升级成"空间安全",视觉和故事都更强。禁区设计见 §6。

---

## 2. 问题设定

### 2.1 动力学(沿用前作 Fossen 模型)

- 位姿 `η = [x,y,z,φ,θ,ψ]ᵀ`,体坐标速度 `ν = [u,v,w,p,q,r]ᵀ`。
- `η̇ = J(η) ν`;`M ν̇ + C(ν)ν + D(ν_r)ν_r + g(η) = τ = B u`。
- `u = [f_1,…,f_M]ᵀ` 为 M 个推进器推力(经 T200 功率图映射)。
- **洋流/扰动显式化**:`ν_r = ν − ν_c`,洋流 `ν_c` 未知但有界 `‖ν_c‖ ≤ c̄`(前作域随机化里已有)。安全证明必须对该扰动鲁棒,否则名义 CBF 一放扰动就假(见 §4.3、§5.3)。

### 2.2 新增:keep-out 禁区

- 禁区中心 `p_o`、半径 `r_o`;机器人等效半径 `r_v`。
- 安全裕度定义碰撞障碍函数:
  ```
  h(p) = ‖p − p_o‖² − (r_o + r_v)²  ≥ 0    (p = η[0:3])
  ```
  安全集 `𝒞 = { x : h(p) ≥ 0 }`,目标是让**控制不变子集 `𝒮 ⊆ 𝒞` 前向不变**(永不离开)。注意目标是 `𝒮` 而非整个 `𝒞`:输入有界时 `𝒞` 的边界附近未必守得住(§5.1)。

### 2.3 约束清单

| 类型 | 约束 | 处理层 |
|---|---|---|
| 空间安全(硬) | `h(p) ≥ 0`(不进禁区) | shield + backup/MPC |
| 推力饱和(硬) | `u_min ≤ u ≤ u_max` | shield + backup/MPC |
| 瞬时功率上限(硬) | `P(f_i) ≤ P_max` | 化为推力箱,见下 |
| 姿态/速度包络(可选,硬) | `|θ|,|φ| ≤ …`,`‖ν‖ ≤ …` | backup/MPC |

> 全部为**硬**约束,由 action inspector + 预测层处理。**本文不设平均功率软约束**(那是前作 PPO-Lag 的做法);平均功率仅作汇报指标。瞬时功率上限是硬约束,已化为推力箱(见下)。

> **技巧**:瞬时功率上限可化为推力箱。因 `P(f)` 关于 `|f|` 单调,`P(f_i) ≤ P_max ⇔ f_i ∈ [f_rev(P_max), f_fwd(P_max)]`,直接并入推力箱,不引入非线性约束。**注意 T200 前反不对称**(如 `[-40.22, 51.50] N`),箱是非对称的。

### 2.4 优化目标

```
在 Fossen 动力学 + 有界洋流下,完成任务(hover 保点 / track 跟参考),
同时对所有 t 硬满足 §2.3 的空间+执行器(含瞬时功率)硬约束(在认证安全域 𝒮 内)。
平均功率仅作汇报指标,不作显式优化目标(与前作 PPO-Lag 不同)。
```

---

## 3. 整体 Pipeline(训练共享,部署分叉 A/C)

### 3.1 推理期(部署)数据流——同一策略,两条部署栈

```
部署-C:  env ─s─▶ 策略 ─u_rl─▶ HOCBF-QP shield ─u_safe─▶ env        (backup 提供可行性,近零开销)
部署-A:  env ─s─▶ 策略 ─u_rl─▶ HOCBF-QP shield ─u_safe─▶ 约束 MPC ─u─▶ env
```

- **两栈共享同一训练好的策略**(训练只挂 shield + backup,见 §3.2);A 的 MPC 仅在**部署期**加入,其 train-deploy 差异也在实测范围内。
- C 每步:策略前向(sub-ms)→ shield QP(热启动/闭式投影)→ 施加 `u_safe`,**保住近零推理开销**。A 再加一个 N 步 OCP。
- shield 不可行的极端情形(理论上不发生于 `𝒮` 内)→ 执行 backup `π_b` 保底;A 的 MPC 不可行同样退回 backup/PID。

### 3.2 训练期数据流(和部署一致,刻意都挂 shield;学习器=纯 PPO)

```
env ──s_t──▶ 策略 ──u_rl──▶ action inspector(HOCBF-QP shield) ──u_safe──▶ env(施加 u_safe)
                 ▲                                                  │
                 │                   纯 PPO(非约束)                │
                 └── 梯度(task reward − w·‖u_safe−u_rl‖²(可选修正惩罚))◀── rollout
```

- **训练期就挂 shield** → 保证**安全探索**(exploration 全程不违约)+ 消除"策略提议不安全、部署时被改写"的 distribution shift。
- shield 为廉价 QP(可闭式投影近似 + 批量化),2048 环境 × 100M 帧下开销可接受;backup 集 `𝒮` 离线构造,在线只查一个约束。
- 策略额外吃**修正惩罚** `‖u_safe − u_rl‖²`,学会主动提议安全动作 → 部署时 **shield 激活率**趋近 0(作为"策略内化安全"的实证指标,见 §8)。

---

## 4. 各模块详解(输入 / 输出 / 职责)

### 4.1 环境:MarineGym + keep-out

| 项 | 内容 |
|---|---|
| **输入** | 推力指令 `u_safe` |
| **输出** | 状态 `s_t`(位姿 `η`、速度 `ν`、上一步动作、任务参考、**到禁区的相对位置 `p−p_o`**) |
| **职责** | GPU 并行仿真刚体+水动力+推进器功率;新增禁区碰撞体;域随机化(沿用前作 Table,含洋流) |
| **改动** | 观测加入禁区相对位置;episode 随机放置禁区,**保证与参考轨迹真冲突**(见 §6) |

### 4.2 策略(actor)—— 纯 PPO,主线 MLP,KAN 作骨干消融轴

| 项 | 内容 |
|---|---|
| **输入** | 状态 `s_t` |
| **输出** | 提议推力 `u_rl`(高斯策略均值,归一化 `[-1,1]^M` → 力) |
| **职责** | 学能量友好、抗扰、**且尽量已满足安全**(靠修正惩罚内化 shield)的策略 |
| **网络** | actor + reward critic `V^r`,**MLP**(沿用前作);**纯 PPO,无 cost critic、无对偶变量 `λ`** |
| **KAN 定位** | **骨干消融轴,不进 headline/贡献**(§8)。仍需警惕:(1) 逼近定理是空贡献(MLP 也万能逼近,K-A 定理只证"存在好逼近"不证"学得更好",与安全命题无关);(2) B-spline 激活定义在固定网格上,RL 里观测分布(含漂移的禁区相对位置/参考/上一步动作)一旦出网格就外推失效、需重拟合 → 训练不稳,MLP 无此失效模式;(3) 深度 RL 中 KAN 实证薄且慢。**正交析因(A/C × MLP/KAN)能把骨干主效应干净分离**,故 KAN 收益由实验回答、而非假设。 |

### 4.3 安全层 action inspector(HOCBF-QP shield)—— 本文核心之一

> 命名:本文的中层安全过滤器称 **action inspector**(= 安全 shield / safety filter),下文两名混用。

**为什么要 HOCBF 而不是普通 CBF**:推力是**二阶**作用(thrust → 加速度 → 速度 → 位置),位置安全约束 `h(p)` 对推力**相对阶为 2**,普通 CBF 失效,必须用高阶 CBF。

构造(相对阶 2):
```
ψ0(p) = h(p)
ψ1(x) = ḣ + α1(h) = 2(p−p_o)ᵀ v + α1(h)        (v = ṗ = R(Θ)ν1,不含 u)
ψ2(x,u) = ψ̇1 + α2(ψ1) ≥ 0
```
其中 `ψ̇1` 含 `v̇ = Ṙν1 + R ν̇1`、`ν̇1 = [M⁻¹(Bu − Cν − Dν_r − g)]_{1:3}` 对 `u` **仿射**,故 `ψ2(x,u) = A(x)u + b(x)`,是 `u` 的**线性**约束。`α1,α2` 取**线性类-𝒦∞**(`α_i(s)=k_i s`),`k_i` 大→响应快但可行域小——这个权衡值得一张图。

**鲁棒化(对付洋流,必做)**:名义 `ψ2≥0` 在扰动下不保安全。用 **ISSf-/robust HOCBF**,把约束收紧为 worst-case:
```
A(x)u + b(x) − Δ(c̄, ‖∂ψ1/∂ν1‖) ≥ 0
```
`Δ` 是有界洋流 `‖ν_c‖≤c̄` 对 `ψ̇1` 影响的上界。安全裕度随扰动收缩但**仍有形式保证**;`Δ` 恰好把 sim-to-real gap 变成一个**可量化的 robustness margin**(加分理论)。

Shield QP:
```
u_safe = argmin_u  ½‖u − u_rl‖²_W
         s.t.  A(x)u + b(x) − Δ ≥ 0             (keep-out,鲁棒 HOCBF)
               u_backup-feasible(x, u)          (存在导回 backup 流的动作,§5.1)
               u_min ≤ u ≤ u_max                (推力饱和 + 瞬时功率上限,§2.3)
```

| 项 | 内容 |
|---|---|
| **输入** | 提议推力 `u_rl`,当前状态 `x`(算 `A,b,Δ`) |
| **输出** | 认证安全推力 `u_safe` |
| **职责** | 单步、最小改动地把动作拉回安全集;在 `𝒮` 上提供**前向不变性证书**(§5) |
| **代价** | 小规模 QP,闭式/主动集/热启动;训练期批量化或闭式投影近似 |

> **可实现推力集(别理想化)**:T200 双向且有死区(`|throttle|<0.075→0`),action→thrust 在零点附近不可逆。QP 在推力空间 `u`(N)求解后要映回 throttle,小推力不可实现——用含死区的**可实现推力集**做保守内近似,或说明用连续松弛并量化误差。

### 4.4 预测/可行性层:两种并列实现 —— backup 策略(C)与约束 MPC(A)

**共同职责**:不是"再跟一遍参考",而是**维持 `x_t ∈ 𝒮`(递归可行性,让 shield 始终可行)+ 捕捉 CBF 单步近视会漏的"当下安全但 N 步后必违约"状态 + 平滑**。两种实现职责相同、机制不同,构成本文的 headline 对比。

- **C —— backup 策略 `π_b`**:一个已知保守控制器(如指向远离禁区的刹车/悬停 PD)。`𝒮 = {x : 从 x 施加 π_b 的闭环轨迹 h(·)≥0 ∀t}`,用其前向积分**隐式定义**不变集。shield 只需保证"存在动作使下一步仍在 `𝒮`"。在线开销≈一次前向积分/查表,**保住近零推理开销**。
- **A —— 约束 MPC**(短时域,重在预测可行 + 平滑):
  ```
  min_{u_0..u_{N-1}}  Σ ‖u_k − u_safe‖²_R + Σ ‖u_k − u_{k-1}‖²_S
  s.t.  x_{k+1}=f_nom(x_k,u_k);  u_min≤u_k≤u_max, P(f)≤P_max
        ψ2(x_k,u_k) ≥ 0(逐步);  终端约束 x_N ∈ 𝒮_f(保递归可行)
        x_0 = s_t   →   输出 u_0;不可行→backup/PID 退化
  ```
  目标是"贴近 `u_safe` + 平滑",**不独立跟踪参考**——RL 仍主导,MPC 只做约束修正与平滑。
  ⚠️ `dt=0.016s`(≈60Hz)下 6-DoF 非线性 + `N≈10–20` 在 Orin Nano 上很紧,**必做实测**——这个实时代价正是 A vs C 要量化的核心之一。

### 4.5 训练:纯 PPO(非约束,不用拉格朗日)

| 项 | 内容 |
|---|---|
| **输入** | rollout `{(s_t, u_rl, u_safe, r_t, s_{t+1})}` |
| **输出** | 更新后的 actor / reward critic `V^r` |
| **目标** | 标准 PPO 裁剪目标最大化任务回报 `J_r`;**可选**加修正惩罚 `− w·E‖u_safe−u_rl‖²` 让策略内化安全——仍是**纯 PPO 的单标量奖励**,非约束、无 `λ`、无 cost critic |
| **职责** | 只学任务;**所有硬安全与功率包络由 action inspector + 预测层保证**。平均功率仅汇报、不优化(与前作 PPO-Lag 本质不同) |

---

## 5. 关键技术点 / 需要证明的东西

1. **前向不变性(主定理,鲁棒版)**:若 `α1,α2` 为类-𝒦∞、初始 `x_0 ∈ 𝒮`、且 shield QP 在 `𝒮` 上带鲁棒裕度 `Δ` 可行,则 `ψ2 ≥ 0` ⇒ `ψ1,h ≥ 0` ⇒ 在 `‖ν_c‖≤c̄` 下 `h(p_t) ≥ 0 ∀t`。**注意前提"QP 可行"必须由 §5.2 闭环,否则定理空转。**
2. **可行性认证(技术命门)**:证明预测/backup 层保持 `x_t ∈ 𝒮`,从而 shield QP **始终可行**——把 §5.1 的前提落地。
   - backup 路线:`𝒮` 由 `π_b` 的流隐式定义 ⇒ 只要下一步能回到 `𝒮`,可行性自持(backup-set CBF 标准结论)。
   - MPC 路线:终端集 `𝒮_f` 控制不变 + 逐步 HOCBF ⇒ 递归可行;MPC 首步 ⊆ shield 安全集,两层永不冲突。
3. **鲁棒裕度**:给出 `Δ(c̄,·)` 的显式上界(洋流对 `ψ̇1` 的影响),说明安全在 `c̄` 内成立、超界时可能破——为 §6 的"证明↔实测一致性"背书。
4. **KAN 收益(骨干消融轴)**:由 A/C × MLP/KAN 析因的实证回答,不做假设、不进贡献。

---

## 6. 需要做的工作清单

**工程**
- [ ] MarineGym 加 keep-out 碰撞体 + 观测加禁区相对位置 + episode 随机放置(**保证与参考真冲突**)。
- [ ] 场景升级:单静态球太弱 → 加**多禁区 + 一个移动禁区** + 一个**专门暴露 CBF 近视的凹形/陷阱构型**(当下安全、N 步后必违约),使预测/backup 层的贡献**可测**。
- [ ] 实现鲁棒 HOCBF-QP shield(可微/批量,训练期闭式投影近似)+ 可实现推力集处理。
- [ ] 实现 backup 策略 + `𝒮` 离线构造(前向积分/可达集)**和** acados 约束 MPC——A、C 两条部署栈都要落地。
- [ ] 训练回路挂 shield + 修正惩罚(A、C 共享此训练);部署回路分叉:C=shield+backup,A=shield+MPC。

**理论**
- [ ] HOCBF 相对阶 2 构造 + 鲁棒前向不变性证明(§5.1)。
- [ ] 可行性认证:backup 不变集 / MPC 递归可行(§5.2)。
- [ ] 洋流鲁棒裕度 `Δ` 的显式界(§5.3)。

**实验**
- [ ] 有禁区(新跑):**C-MLP / A-MLP** 铺满 3 车×4 任务;消融 **去-shield / 去-预测层** + **MLP↔KAN** 骨干切片。前作 PPO / PPO+penalty / PPO-Lag **无禁区数据直接复用不重跑**(边界见 §8.2)。
- [ ] **证明↔实测一致性(关键)**:别报"零违约"再出现非零。定义**认证安全域 `𝒮`**,证明违约**只**发生于 (i) 初始在 `𝒮` 外 或 (ii) 洋流超 `c̄`——用 §5 的分析解释每一次违约。"理论预测哪破、实测正好在那破"本身是强贡献。
- [ ] Jetson profiling:报告 **C(shield+backup)** 与 **A(shield+MPC)** 两档;诚实量化预测式安全的实时代价。

---

## 7. Contributions(精炼版)

1. **首个面向水下 6-DoF 的逐步硬安全 RL**:统一空间禁区 + 双向推力/瞬时功率包络,针对**推力相对阶 2**构造 HOCBF,并在**输入受限 + 有界洋流**下证前向不变(非名义理想)。
2. **可行性认证的分层架构 + 两种实现的受控对比**:上层瞬时投影证书 + 下层维持控制不变集 `𝒮`(证明 shield **始终可行**),并给出该层的**两种并列实现**——廉价 backup 滤波(C)与约束 MPC(A);在**同一策略、同一 shield** 下 head-to-head,量化预测式安全的**收益(近视违约↓/smoothness↑)与实时代价**。把"两层冗余"质疑转成"可行性 vs. 瞬时投影"的精确分工。
3. **model-free 精度 + 粗模型安全的正交分解**:精确跟踪由 RL 学,硬安全仅用保守过近似动力学 + 鲁棒裕度,化解与前作的张力,并把 sim-to-real gap 量化为 robustness margin。
4. **同一 MarineGym testbed 跨三车四任务 + 真冲突禁区**的受控对比 + Jetson 实测:量化硬安全代价、验证安全探索(全程近零违约)、并展示证明与实测的一致性。

---

## 8. 实验设计

### 8.1 方法矩阵(两个正交轴 + 复用前作对照)

- **可行性/预测层轴**:{ **C** = shield+backup, **A** = shield+MPC } —— **headline 对比**。
- **策略骨干轴**:{ **MLP**, **KAN** } —— **骨干消融**(不做贡献,只回答"骨干是否有收益")。
- 正交析因(A/C × MLP/KAN)能把两轴主效应**干净分离**,故 KAN 收益由实验回答,而非假设。

| 类别 | 配置 | 环境 | 来源 / 算力 |
|---|---|---|---|
| 前作对照 | PPO / PPO+penalty / PPO-Lag | **无禁区** | **复用前作数据,零训练** |
| 主线(新跑) | Ours **C-MLP** / **A-MLP** | 有禁区 | 全 3 车 × 4 任务 × 3 seed |
| 骨干消融(新跑) | **C-KAN**〔/ A-KAN 查交互〕 | 有禁区 | **代表性切片**(固定层=C,如 1 车×4 任务 或 3 车×1 任务) |
| 安全消融(新跑) | **去-shield** / **去-预测层** | 有禁区 | 全设定或切片 |

> **别爆算力**:full factorial(6 方法 × 12 设定 × 3 seed ≈ 216 训练)不做。headline 的 A/C 铺满 12 设定;MLP/KAN 与安全消融只在切片上跑(ablation 不需铺满)。

### 8.2 前作数据的复用边界(关键,别踩)

前作 baseline 在**无禁区**环境跑;加 keep-out 禁区 + 禁区相对观测后是**另一个 MDP**:

- **无禁区任务指标**(功率/smoothness/跟踪/成功率/TTG)→ **直接复用前作数字**,做"能量延续 + shield 不损任务"的对照。
- **有禁区安全指标**(违约率/最小距禁区)→ 前作**无此数据**(没禁区就没违约),且 task-only PPO 会直接穿过障碍、老指标不可比 → **不能复用**。
- **有禁区的"不安全参照"**由**去-shield 消融**提供(同一策略、关 shield、带禁区),而**非重跑前作 baseline**——控制了策略变量,归因更干净;可选在 1–2 设定上补 plain-PPO-带禁区,佐证"标准 RL 也不安全"。
- **保留一组无禁区对照**(直接嵌前作数据):近乎白送地展示 shield 零成本、继承前作。

### 8.3 表格与图

- **主表(有禁区)**:任务 × 车 × 方法{ 去-shield, C-MLP, A-MLP(+骨干切片) },列 = {违约率↓, 最小距禁区↑, **shield 激活率↓**(策略内化安全), **backup/MPC 触发率**(可行性边界被逼近的频率), 成功率↑, 跟踪 RMSE↓, smoothness↓, 平均功率↓, TTG↓}。
- **续表(无禁区)**:嵌前作 PPO / PPO+penalty / PPO-Lag + 本文,列 = {功率, smoothness, 跟踪, 成功率, TTG},展示 shield 零任务成本。
- **A vs C(headline)**:同一策略、同一 shield 下,MPC(A)相对 backup(C)的近视违约↓ / smoothness↑ 收益,与实时开销↑ 代价。
- **骨干消融**:C-MLP vs C-KAN(固定层),看 KAN 是否更省参/更快/更平滑;无收益就一句话带过。
- **训练曲线**:return / 违约率 / 平均功率 / smoothness,验证**安全探索**(全程近零违约)。
- **轨迹图**:三车四任务叠画各方法,可视化"绕开禁区、动作平滑但任务不退化";特别画凹形陷阱构型下"单步 CBF 撞墙、加预测层绕出"的对比。
- **Jetson**:参数量、推理时延(策略 vs shield vs MPC 三档)、CPU/GPU/内存/板功耗;报 C 与 A 两档实时代价。

---

## 9. 风险与注意事项

- **可行性(最致命)**:输入受限 + 相对阶 2,shield QP 可能无解;§5.2 的可行性认证**必须闭环**,否则主定理空转。这是审稿人第一刀。
- **洋流鲁棒性**:名义 CBF 一放扰动就假;§4.3/§5.3 的鲁棒裕度必做。
- **model-free 张力**:§0 的"粗模型只保守、精度交给 RL"论证要写进 intro,别等审稿人问。
- **预测层 vs 实时**:A(MPC)打破前作"零开销"卖点,C(backup)保住;二者代价差正是 headline 要实测的。MPC 超时则降 N / 用 acados / 只在近禁区激活。
- **架构对比要主动交代**:§1 的 A/C 权衡 + "可行性 vs 投影"分工写进 intro;A、C 并列且互为对照,天然回应"为什么要预测层 / 哪种更值"。
- **KAN 装饰化**:作骨干消融轴、不进 headline/贡献;正交析因保证归因干净(§8.1)。
- **禁区放置**:必须与任务参考**真冲突**,否则安全模块无事可做,实验没说服力。

---

## 10. 实测进展与立论修正(2026-07-09,MarineGym / Track / BlueROV)

> 本节是**基于真实实验**对 §1–§9 原计划的修正。部分设计(尤其单步 shield 作为主贡献)已被实测否定,以此为准。

### 10.1 已落地的代码
- **真避障任务环境**(`marinegym/envs/single/track.py` + `cfg/task/Track.yaml`,搜 `keepout`):球形 keep-out(数学球,无物理碰撞体,`radius`/`vehicle_radius`)、**障碍相对位置 `p_o−p` 进观测**(`obstacle_in_obs`,公平)、**避障接近惩罚 reward**(`reward_weight`,二次,让它成为真任务)、`collision`/`min_obstacle_dist` stats;障碍放在参考轨迹上、spawn-safe、每 episode 随机相位。
- **arm A 单步 HOCBF shield**:`marinegym/utils/action_inspector.py`(默认关)。
- **arm B MPPI 预测滤波**:`marinegym/utils/mppi_filter.py` —— `MPPIShield`(粗糙平移模型)与 `MPPIExactShield`(精确 6-DOF 模型),`keepout.mppi.enable` / `mppi.exact`。
- **精确名义动力学**:`marinegym/utils/nominal_dynamics.py`,一步预测误差 **<2%**(验证钩子 `keepout.validate=true`)。

### 10.2 关键实测结论(BlueROV,radius=1.3,真任务:障碍可见+避障奖励)
| 方法 | collision↓ | 判读 |
|---|---|---|
| **baseline(纯 PPO + 避障奖励)** | **~0.39** | RL 本来就避得不错 |
| arm A shield(naive/力矩中性/调参) | 0.67–0.97 | **更差**:单步来不及刹车 + 饱和产生力矩把载具翻滚 |
| arm B 粗 MPPI | ~0.50 | 打平/略差 |
| arm B **精确** MPPI | ~0.44(初步、数据稀) | 打平,且**慢** |

**核心事实:in-distribution 下,model-free RL(带避障奖励 + 障碍观测)避障已足够强,任何 filter 顶多打平;单步 shield 反而有害**(实证了 §5.1「输入受限点无法回头」)。~35 次 Isaac 实验支撑。

### 10.3 立论修正(最重要)
- **collision(in-distribution)不是能体现 filter 价值的指标**——注定打平。
- **filter(尤其精确 MPPI)的价值 = 硬保证 + 泛化/鲁棒**:model-based、不靠学,换布局/加洋流照样守住。
- **正确实验协议(filter 唯一可能碾压的战场)**:RL 训练在障碍集 A → **测试在更难/未见障碍布局集 B(或强洋流)** → 赌 RL 软避障**泛化崩**、精确 MPPI **守住**。详见 §10.5。
- **方向已定 = 路线 (a) OOD 评测**,全力做。**停止判据**:若 OOD 上 filter 相对 baseline 无显著优势、且调参后仍无 → **直接放弃 filter 方向**(不做 benchmark 兜底)。

### 10.4 精确模型工程要点(踩坑记录,复现必看)
- **别重实现、要对齐仿真自己的力**。误差从 ~90% 降到 <2% 的关键修复:
  1. **浮力帧翻转**——仿真在**翻转后的 rpy**(`[1,2]*=-1`)上算浮力、再 `buoy[[1,2,4,5]]*=-1` 翻回;我最初漏了这步(浮力 ~108N≈重力,帧错就差 ~200N)。
  2. 用 **M_RB 积分**(附加质量作为**有限差分力** `-M_A·acc_filt`,`acc_filt` 在翻转帧、0.7/0.3 低通),**不是** M_RB+M_A 隐式。
  3. **T200 执行器迟滞**必须建(throttle `+= 0.43·(a−throttle)`,rpm 一阶滞后);我的 T200 重实现验证是对的。
- **精确 MPPI 很慢**:K×N 全模型重滚 × train_every 步/迭代 → full(K=128,N=20)~24min/迭代。正式用要降 K/N、`algo.train_every` 减小、或只在近障碍/部署期启用。

### 10.5 实验计划:OOD 泛化评测(路线 a,已定)

**关键可行点:精确 MPPI 只在评测/部署期挂,不进训练回路** → 慢不影响(评测就几十个 episode),绕开训练吞吐问题。

**① 训练(in-distribution,障碍集 A)**
- 纯 PPO + 避障奖励,障碍集 A = { 单障碍、`radius ∈ [0.5,1.0]`、放在参考轨迹上、洋流在训练域 `c̄` 内 },训到收敛,得 baseline 策略 `π`(学会躲 A 类)。
- 主对比用**同一 `π`**(训练期不挂 filter);可另训一版训练期挂精确 MPPI 作对照。

**② 评测(OOD,障碍集 B,策略没见过)—— 同一 `π`,{无 filter} vs {精确 MPPI}**
- **B1 更大障碍**:`radius ∈ [1.5,2.5]`;
- **B2 偏离参考的横向障碍**(策略没学过的构型,`lateral_offset_std>0`);
- **B3 强洋流**:超训练域 `c̄`。
- 每设定 100 seed,报 **collision↓ / 最小距障↑ / 成功率↑ / 跟踪 RMSE**。

**③ 判据**
- **成功**:B 上 `π`(无 filter)collision 显著上升(泛化崩),`π` + 精确 MPPI collision≈0 或显著更低 → filter 价值立住,强论文。
- **失败**:B 上 filter 无显著优势,且调 `K/N/w_coll/margin/horizon` 后仍无 → **放弃 filter 方向**。

**④ 最简先行(先看效果,再决定要不要 B2/B3)**
- 只做 **B1 单障碍**:train 小障碍(`radius 0.5–1.0`)存 ckpt → `load_ckpt` 载入、eval 大障碍(`radius ~2.0`),`filter on/off` 比 collision。**多障碍(原 B2)不做。**
- 有效果再扩 B2(横向偏离)/B3(强洋流);没效果、调参也不行 → 放弃。
- [ ] **评测脚本**:同一 checkpoint,filter on/off。
- [ ] 精确 MPPI 提速(向量化 rollout / 降 N / 近障碍才启用)——评测吞吐够用即可。

---

## 11. 方法定稿:Predict→Protect→Internalize(A+B+C,2026-07-10 实证成立)

### 11.1 三组件
```
obs(+risk) ─▶ [PPO 策略] ─u_rl─▶ [A 风险预测器] ─触发?─▶ [B MPPI 预测滤波] ─u_safe─▶ env
                  ▲                (精确模型单条前滚 H步)     (精确模型 K 采样,仅触发时)
                  └──────────── C 内化:reward −w‖u_safe−u_rl‖² + risk 进观测 ─────────┘
```
- **A RiskMonitor**(`marinegym/utils/risk_monitor.py`):<2% 精确模型把策略动作前滚 H 步(动态障碍匀速外推),输出风险标量(进观测)+ 触发 B。
- **B MPPIExactShield**(`mppi_filter.py`):A 触发才启动;安全批整批跳过。
- **C 内化**(`internalize_weight`):惩罚 B 的修正 → 激活率下降,部署近零开销。

### 11.2 关键场景:动态拦截障碍(静态场景已被 PPO"打穿",无差异空间)
- 静态单障碍+速度/位置观测:**纯 PPO 一轮学会零违约**——任何组件无增量(60 iters 实测)。
- **动态拦截障碍**(`keepout.dynamic`):障碍从侧向以 0.4–0.9 m/s 瞄准载具未来 150–350 步参考位置发射、按时相遇,每 200 步重瞄——反应式躲不掉,必须预判。观测公平给 PPO 相对位置+速度。

### 11.3 组件阶梯(radius=0.8,60 iters,收敛段)
| 方法 | coll↓ | minDist↑ | filtAct | 训练累计违约 |
|---|---|---|---|---|
| PPO | 0.023 | 1.93 | — | 0.23 |
| B-only(距离门控) | 0.025 | 1.92 | — | 0.23 |
| A+B(风险门控) | 0.015 | 1.79 | 0.123 | 0.06 |
| **A+B+C** | **0.000** | **2.39** | **0.072** | **0.00(全程零,含随机初期)** |

**每个组件的可测增量**:距离门控在动态下失效(B-only≈PPO)→ **A 的预测触发是 B 生效的前提**;C 使激活率再降 42% 且违约归零。**三支柱故事:①safe exploration(全程零违约)②残留违约消除(0.023→0)③内化(激活率降、部署近零开销)。**

### 11.4 待巩固
- [x] 双 seed × 120 iters 巩固(进行中)
- [ ] 统计显著性(每设定 ≥3 seeds/100 eval episodes)、episode_len 差异说明(A+B+C 活得更久,stats 样本少)
- [ ] 消融补全:A-only(只报警不滤波)、C 权重扫描、risk horizon 扫描
- [ ] 提速与开销表:filter 激活率 × MPPI 成本 = 实际开销(部署近零)
