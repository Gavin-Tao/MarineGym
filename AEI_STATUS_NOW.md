# AEI 当前状态（2026-08-25 11:10）

## 1. 正在跑什么

`aei_r1a_matched.sh` —— 把 Predictive+binary 这一格的触发边界从 `risk.threshold=0.3`
对齐到 `0.6`，与本方法的 `mppi.soft_hi=0.6` 一致。对齐后两格只差「接管律」。

| 时刻 | 事件 |
|---|---|
| 09:10 | 训练开始 |
| 10:28 | 训练完成 rc=0，checkpoint_final.pt 已落盘 |
| 10:33~11:01 | 评测 s1~s5 完成 |
| 进行中 | s6，之后 s7~s10 |
| ~11:39 | 这一阶段预计结束 |

稳定 6.8 分钟/次评测。

## 2. 前 5 个 seed 的预览结果（不是最终值）

| 指标 | 你的方法 soft 0.6 | r1a_t06 binary 0.6 | 旧 binary thr=0.3 |
|---|---|---|---|
| collision ↓ | 0.0169 | **0.0131** | 0.0155 |
| return ↑ | 29.513 | **31.19** | 30.322 |
| detour ↓ | 0.2215 | **0.1202** | 0.2665 |
| engagement | 0.0176 | 0.0265 | 0.0164 |

r1a_t06 逐 seed：
- collision: 0.0000 / 0.0146 / 0.0221 / 0.0146 / 0.0143
- return:    32.57 / 30.28 / 30.71 / 31.36 / 31.01
- detour:    0.1239 / 0.1221 / 0.1180 / 0.1184 / 0.1186

**三个指标上，对齐后的阶跃接管都优于斜坡接管。**

判读：
- collision 差 0.0038，单 seed std 约 ±0.010 → **仍在噪声内**，等 10 seed 做检验
- return 差 1.68，std 约 0.6 → **可能显著**
- detour 差近一倍，std 仅 0.007~0.010 → **几乎肯定显著**

## 3. 一个被推翻的预测

我此前判断「binary 边界从 0.3 挪到 0.6 会更重手、detour 更高」。实测相反：
0.2665 → 0.1202，降了一半以上。

机制解释：更早介入（0.6）时离障碍还远，MPPI 用很小的修正即可化解；
在 0.3 才介入时已经贴近，只能剧烈规避。**更早介入 = 更小的总偏离。**

推论：旧那格 thr=0.3 的高 detour 主要来自阈值过紧，而非「阶跃接管」本身。
对齐边界后，阶跃的这个缺点基本消失。

注意：r1a_t06 是用 0.6 门控**重新训练**的策略，detour 差异里既有滤波器作用，
也有策略自身适应，两者目前分不开。

## 4. 后续排期

| 阶段 | 预计完成 |
|---|---|
| r1a_t06 评测 s6~s10 | 11:39 |
| 落地 risk.gate 补丁 + Track.yaml | 11:44 |
| r1b_v2 训练（risk.gate=false，obs 45） | 13:02 |
| r1c_v2 训练 | 14:20 |
| r1b_v2 评测 ×10 | 15:28 |
| r1c_v2 评测 ×10 | 16:36 |
| 按冻结参数集重出全部图表 | **≈16:40** |

## 5. 已就绪但未落地的东西

- `scripts/aei_fixed_params.sh` —— 唯一冻结的超参集合，所有消融格子 source 它
- `scratchpad/apply_riskgate.py` —— risk.gate 补丁（缩进 bug 已修、语法通过、六格逻辑测试全过）
- `scripts/aei_geom_refit.sh` —— r1b/r1c 重跑脚本
- `scripts/aei_equiv_check.sh` —— 等价性验证（你说不做，留存）

`marinegym/` 与 `cfg/` 目前对 HEAD **零差异**，补丁只在 scratchpad 副本里。

## 6. 如果这个方向保持住

若 10 个 seed 齐了之后阶跃仍全面优于斜坡，需要面对：soft blend 在当前证据下没站住。
可选方向：
1. 换指标 —— 斜坡的理论优势在**动作平滑度**上（`action_smoothness`、`correction` 幅度），
   这两栏现在图里根本没画。这是唯一还没检验过的、对 soft blend 有利的角度。
2. 接受结果，把卖点从「斜坡接管」挪到别处（例如训练期安全探索）。
