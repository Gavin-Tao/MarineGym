# 接管律对比结果：斜坡 vs 阶跃（边界已对齐 0.6）

日期 2026-08-25 11:40 ｜ 场景 ood ｜ 各 10 个评测 seed ｜ 唯一变量 = `mppi.soft_blend`

- **RP-PSF (ours)**：`soft_blend=true`，λ 随预测间隙从 0.6→0 线性升到 1
- **Predictive + binary**：`soft_blend=false`，`risk.threshold=0.6`，触发即满力接管

两者触发边界同为 0.6，观测同为 45 维，其余超参逐项相同（见 `scripts/aei_fixed_params.sh`）。

## 结果

| 指标 | 方向 | 斜坡 (ours) | 阶跃 @0.6 | t | p | 谁优 |
|---|---|---|---|---|---|---|
| collision | 低好 | 0.01687±0.010 | 0.01527±0.009 | 0.35 | 0.734 | 打平 |
| return | 高好 | 29.513±0.534 | **30.751±0.784** | −3.92 | 0.0012 ** | 阶跃 |
| detour_ratio | 低好 | 0.2215±0.010 | **0.1197±0.004** | 29.05 | <0.0001 *** | 阶跃 |
| min_obstacle_dist | 高好 | 6.680±0.141 | 6.650±0.185 | 0.39 | 0.701 | 打平 |
| **action_smoothness** | 高好 | **−0.7829±0.006** | −0.8116±0.008 | 8.71 | <0.0001 *** | **ours** |
| **correction** | 低好 | **0.0220±0.014** | 0.0357±0.013 | −2.13 | 0.048 * | **ours** |
| **tracking_error_ema** | 低好 | **1.3484±0.002** | 1.3540±0.002 | −6.60 | <0.0001 *** | **ours** |

碰撞 episode 级两比例检验：ours 24/1429 = 1.68% vs 阶跃 21/1373 = 1.53%，z=0.32，**p=0.75**。

## 判读

**安全性完全打平**（p=0.75）。这很关键：接管律的选择**不影响安全**，
所以这不是「安全 vs 性能」的取舍，而是「用什么方式达到同样的安全」。

在同样的安全水平下，两种接管律给出两种不同的代价结构：

**阶跃赢在任务效率**
- return 高 1.24（p=0.001）
- detour 只有一半（0.120 vs 0.222，p<0.0001，t=29）

**斜坡赢在执行质量**
- 动作更平滑（p<0.0001）
- 修正幅度小 38%（0.0220 vs 0.0357，p=0.048）
- 跟踪误差更低（p<0.0001）

## 机制解释（自洽）

斜坡在 0~0.6 整个区间持续做**小幅**修正 → 路径连续弯曲（detour 高）
但从不大幅偏离参考（tracking_error 低）、动作连续（smoothness 高）、单次修正小。

阶跃只在触发时**满力**接管 → 介入次数少、总路径短（detour 低、return 高），
但每次都是动作突跳（smoothness 差）、修正幅度大、瞬时偏离大（tracking_error 高）。

一句话：**斜坡 = 持续的小偏离；阶跃 = 罕见的大偏离。安全性相同。**

## 结论：不建议把阶跃改成 ours

1. **安全打平**，headline 的安全主张不受接管律影响，换不换都一样
2. 斜坡赢 3 项、阶跃赢 2 项，不是一边倒
3. 斜坡赢的那 3 项**正是 soft blend 被设计出来要解决的**（平滑接管、小修正），
   故事自洽，不是事后找补
4. 对真实 AUV，执行质量有物理意义：推进器磨损、功耗、饱和裕度。
   而 detour/return 的差距在仿真里显著，在实机上未必是同等量级的痛点
5. 换成阶跃等于放弃 RP-PSF 里 "Risk-Proportional" 的方法身份，
   且 fig2（soft_hi 敏感性）整张作废

## 必须诚实报告的部分

阶跃在 return 与 detour 上的优势是**真实且显著**的，不能不写。
建议的写法是把它作为**权衡**呈现，而不是隐去：

> 在同等安全水平下（p=0.75），比例接管以更长的绕行路径为代价，
> 换取显著更平滑的执行、更小的修正幅度与更低的跟踪误差。

## 图表待办

fig3 目前只画 collision / return / engagement，**把 ours 最强的三栏全漏了**。
需加入 `action_smoothness`、`correction`、`tracking_error_ema`。
